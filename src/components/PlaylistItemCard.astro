---

import type { Playlist } from "../lib/data";
interface Props {
    playlist:Playlist
}
const {playlist} = Astro.props
const {id, cover, title, artists, color} = playlist
const artistsString = artists.join(", ")
---


<a
href=`/playlist/${id}`
class="playlist-item flex flex-col text-zinc-200 transition-all duration-100 shadow-lg hover:shadow-xl hover:bg-zinc-800 relative p-2 overflow-hidden  gap-4 rounded-md bg-zinc-500/30 w-40 "
transition:name=`playlist ${id} box`
>
    <picture class="flex-none aspect-square w-full h-auto">
        <img src={cover} alt={`Cover of ${title}`} class="object-cover w-full h-full rounded-md " transition:name=`playlist ${id} image`>
    </picture>
    <div class="flex flex-auto flex-col px-2 truncate">
        <h4 transition:name=`playlist ${playlist?.id} title`>
            {title}
        </h4>
        <span class="text-xs text-gray-400" transition:name=`playlist ${playlist?.id} artists`>
            {artistsString}
        </span>
    </div>
</a>